apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-results-pvc
spec:
  storageClassName: standard  # Uses minikube's built-in provisioner
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
